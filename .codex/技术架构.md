# 技术架构规则

## 1. 核心技术栈

- Next.js App Router
- TypeScript
- React
- Tailwind CSS
- shadcn/ui
- Motion for React（优先使用 `motion`）
- PWA：Manifest + Service Worker

不使用 Vite 作为主构建工具，不维护 Next.js + Vite 双构建链。

## 2. 静态优先

V1 固定采用：

> Build Time Data + SSG + Client Interaction + PWA

Next.js 目标配置：

```ts
output: "export"
```

最终必须能够输出可直接托管的 `out/`。

除非未来明确调整部署架构，否则禁止把以下能力作为核心依赖：Server Actions、运行时 SSR、服务端 Session、依赖 Next.js Server 的 API Route、服务端动态图片处理、数据库实时查询渲染。

需要交互时优先选择客户端能力、构建时数据或独立外部 API。

## 3. 数据驱动

禁止在页面中大量硬编码业务数据。数据流程参考 `zero-blog`：

```text
内容源 / 静态数据
      ↓
build-site-data.ts
      ↓
标准化 JSON
      ↓
Next.js SSG
      ↓
out/
```

展示内容应尽可能由统一 Schema 驱动，Schema 不得强耦合具体 React Component 实现。

## 4. 数据模型

### Component

核心字段建议：

```ts
export interface VibeComponent {
  slug: string
  name: string
  description: string
  type: "business" | "app-ui" | "motion"
  tags: string[]
  preview: { cover: string; demo?: string }
  prompt: { content: string; language?: "zh" | "en" }
  stack: string[]
  features?: string[]
  relatedTemplates?: string[]
  status?: "draft" | "published"
  sort?: number
}
```

### Template

```ts
export interface VibeTemplate {
  slug: string
  name: string
  description: string
  cover: string
  demo?: string
  tags: string[]
  pages?: string[]
  features: string[]
  stack: string[]
  relatedComponents?: string[]
  solutions: TemplateSolution[]
  status?: "draft" | "published"
  sort?: number
}

export interface TemplateSolution {
  type: "static" | "fullstack" | "admin" | string
  name: string
  price: string
  description?: string
  stack: string[]
  includes?: string[]
  suitableFor?: string
}
```

报价是展示文案而非交易金额，统一使用字符串，例如 `¥299 起`、`联系报价`。

## 5. 建议目录

```text
vibecode/
├── app/
├── components/
│   ├── ui/
│   ├── layout/
│   ├── gallery/
│   └── common/
├── data/
│   ├── components/
│   │   ├── business/
│   │   ├── app-ui/
│   │   └── motion/
│   └── templates/
├── generated/
│   ├── components.json
│   ├── templates.json
│   └── search-index.json
├── public/
│   ├── covers/
│   ├── previews/
│   ├── demos/
│   ├── icons/
│   └── manifest.webmanifest
├── scripts/
├── lib/
├── types/
├── docs/requirements/
└── .codex/
```

目录可按实际开发演进，但数据源、生成数据、页面、展示组件和部署脚本职责必须清晰。

## 6. PWA

至少包含：

- `manifest.webmanifest`；
- App icons；
- `display: standalone`；
- theme / background color；
- 基础 Service Worker；
- 合理的静态资源缓存。

PWA 是网站附加能力，不能破坏普通浏览器访问、SEO 或 Static Export。离线策略保持简单可靠，不要求缓存所有页面。

## 7. SEO

每个 Component 和 Template 必须拥有独立、稳定、可索引的 URL，并至少提供 title、description、canonical、Open Graph、sitemap 和 robots.txt。

按内容规模可继续生成 `llms.txt`、Search Index、RSS / Feed。
